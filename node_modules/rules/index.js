module.exports = {
    Admin : {
            Admin_name: {
                // 是否必填,传入的值为 undefined null 空字符 空数组，空对象 都通不过验证。
                presence: { allowEmpty: false },
                // 类型
                type: 'string',
                // 长度
                length: {
                    minmum: 1, // 最小长度
                    maximum: 10 // 最大长度
                },
            },
            Admin_pass: {
                presence: { allowEmpty: false },
                type: 'string',
                length: {
                    minmum: 6,
                    maximum: 10
                }
            }
    }
}